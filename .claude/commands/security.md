---
description: Run the security pass (threat model + headers + deps)
argument-hint: "[feature]"
---

Run the `security-pass` skill for: $ARGUMENTS

Work through its procedure with me: the five-minute threat-model questions, security-header
verification, and confirmation the dependency scan ran. Surface the abuse cases only someone
who knows the feature's intent can see. See ../../CLAUDE.md.
