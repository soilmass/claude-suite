---
description: Re-verify the perishable stack facts
argument-hint: ""
---

Invoke the `perishable-refresh` skill to re-verify the dated specifics — OWASP ordering, Core
Web Vitals metrics and thresholds, tool versions, the Drizzle/Clerk/edge-driver standings —
against current sources.

Let the skill present a diff of what changed in the world vs what the canon says and propose
updates to `../../CLAUDE.md` and the reference. It never rewrites the source of truth silently,
and it leaves the durable principles (the spine, the nine rules) untouched.
