---
description: Run the accessibility gate (axe + manual WCAG 2.2 AA)
argument-hint: "[path or url]"
allowed-tools: Bash, Read, Grep, Glob
---

Run the `a11y-gate` skill against: $ARGUMENTS

Follow its procedure: axe over the rendered output, then the manual WCAG 2.2 AA items axe
cannot detect (meaningful alt text, reading order, keyboard flows). Name the concrete fix for
each finding. See ../../CLAUDE.md for the AA floor.
