---
description: Plan a feature as a vertical slice
argument-hint: "[feature]"
---

Plan this feature as a vertical slice: $ARGUMENTS

Spawn the `feature-planner` agent and thread the feature through it. The plan stays on the
spine and nine rules in ../../CLAUDE.md — do not restate them; the agent enforces them.
